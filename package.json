{
  "name": "pantry",
  "private": true,
  "version": "0.0.0",
  "description": "Household inventory PWA monorepo",
  "packageManager": "pnpm@10.4.1",
  "scripts": {
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "pnpm -r --if-present test"
  }
}
